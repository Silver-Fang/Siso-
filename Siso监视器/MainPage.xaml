<Page x:Class="Siso监视器.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Siso监视器"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
	<Page.TopAppBar>
		<CommandBar>
			<AppBarButton x:Name="载入小程序" Label="载入小程序" Icon="Upload"/>
			<AppBarButton x:Name="选择Silicon文件夹" Label="选择Silicon文件夹" Icon="OpenLocal"/>
			<AppBarElementContainer>
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition/>
						<ColumnDefinition/>
						<ColumnDefinition/>
					</Grid.ColumnDefinitions>
					<TextBlock x:Name="Silicon运行时" Grid.Column="0"/>
					<ComboBox x:Name="帧缓冲区数" Header="帧缓冲区数" Grid.Column="1"/>
					<ComboBox x:Name="采集超时" Header="采集超时" Grid.Column="2"/>
				</Grid>
			</AppBarElementContainer>
			<AppBarButton x:Name="选择临时文件夹" Label="选择临时文件夹" Icon="OpenLocal"/>
			<AppBarElementContainer>
				<TextBlock x:Name="临时文件"/>
			</AppBarElementContainer>
		</CommandBar>
	</Page.TopAppBar>
	<MediaPlayerElement/>
</Page>
